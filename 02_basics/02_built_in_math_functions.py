# ==================== Built-in Math Functions ====================

"""
Built-in Math Functions:
- Standard functions available in Python's global namespace without importing any module.
- Core functions:
    - round(number, ndigits): Rounds to nearest integer or specified decimal places.
    - abs(number): Returns the absolute (non-negative) distance of a number from zero.
    - pow(base, exp): Computes base raised to the power exp (equivalent to base ** exp).
    - max(iterable / args): Returns the largest value.
    - min(iterable / args): Returns the smallest value.
"""

x = 3.14
y = -4
z = 5

# Rounds to the nearest whole integer (3.14 -> 3)
# result = round(x)

# Computes absolute value (-4 -> 4)
# result = abs(y)

# Calculates 4 raised to power 3 (4 * 4 * 4 = 64)
# result = pow(4, 3)

# Finds maximum value among arguments (max of 3.14, -4, 5 -> 5)
# result = max(x, y, z)

# Finds minimum value among arguments (min of 3.14, -4, 5 -> -4)
# result = min(x, y, z)

# print(result)


